// Тестируем, что 1+1=2
console.log('Начинаем тесты...');

// Тест 1: Проверяем математику
if (1 + 1 === 2) {
  console.log('✅ Тест 1 пройден: 1+1=2');
} else {
  console.log('❌ Тест 1 не пройден');
  process.exit(1); // Останавливаем программу с ошибкой
}

// Тест 2: Проверяем работу счетчика (имитация)
let testCounter = 0;

// Увеличиваем счетчик
testCounter++;
if (testCounter === 1) {
  console.log('✅ Тест 2 пройден: счетчик увеличился');
} else {
  console.log('❌ Тест 2 не пройден');
  process.exit(1);
}

// Уменьшаем счетчик
testCounter--;
if (testCounter === 0) {
  console.log('✅ Тест 3 пройден: счетчик уменьшился');
} else {
  console.log('❌ Тест 3 не пройден');
  process.exit(1);
}

console.log('🎉 Все тесты пройдены успешно!');